---
phase: 05-build-types-docs-expo-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - TYPE-01
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05

must_haves:
  truths:
    - "`npm run build` produces `lib/module`, `lib/commonjs`, and `lib/typescript` with zero errors (BUILD-02, TYPE-01)"
    - "`npx tsc --noEmit` passes with `strict: true`, proving zero Node.js API usage in src (BUILD-01)"
    - "package.json `exports` field contains a `react-native` source condition plus `types`/`import`/`require` conditions (BUILD-03)"
    - "package.json `peerDependencies` declares `react`, `react-native`, and `react-native-tcp-socket` (BUILD-04)"
    - "package.json `dependencies` contains exactly the 4 already-present runtime deps and no others (BUILD-05)"
  artifacts:
    - package.json
    - package-lock.json
    - lib/module/index.js
    - lib/commonjs/index.js
    - lib/typescript/src/index.d.ts
  key_links:
    - "`exports[\".\"].react-native` → `./src/index.ts` (Metro consumes TS source directly)"
    - "`main`/`module`/`types` → `./lib/commonjs/index.js` / `./lib/module/index.js` / `./lib/typescript/src/index.d.ts`"
    - "`react-native-builder-bob` config block → `src` source dir → `lib` output dir"
---

<objective>
Make the library buildable with TypeScript + react-native-builder-bob and make package.json a publish-ready contract. `npm run build` produces dual CJS + ESM output plus shipped `.d.ts` declarations, and package.json declares correct `exports` conditions, `files`, and `peerDependencies` while keeping runtime deps at the four already present.

Purpose: This is the phase's tracer — prove the build architecture end-to-end (one command → compiled artifacts) before the Expo plugin and README build on top of it.
Output: A buildable package with `lib/` outputs and a package.json that satisfies BUILD-01 through BUILD-05 and TYPE-01.
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md  (SOURCE OF TRUTH — builder-bob config block, tsconfig target, peer deps, exports/files template)
@.planning/ROADMAP.md  (Phase 5 success criteria #1-#4)
@.planning/REQUIREMENTS.md  (BUILD-01..05, TYPE-01)
@package.json  (current state: main/module/types set, 4 runtime deps, typescript/jest/ts-jest/@types/debug/@types/events devDeps, build script is an echo stub)
@tsconfig.json  (already matches STACK.md: target esnext, moduleResolution bundler, declaration true)
@src/index.ts  (barrel export — the public API surface whose .d.ts must be shipped)
</context>

## Artifacts this phase produces

Full phase artifact inventory (this plan produces the package.json + lib/* subset; 05-03 produces the plugin files; 05-02 produces README.md):

- `package.json` (updated: builder-bob devDep + config, `exports`, `files`, `peerDependencies`, `source`/`react-native` fields, real `build` script) — THIS PLAN
- `package-lock.json` (updated by `npm install`) — THIS PLAN
- `lib/module/*`, `lib/commonjs/*`, `lib/typescript/*` (builder-bob build output) — THIS PLAN
- `app.plugin.js`, `plugin/src/index.ts`, `plugin/src/withAndroid.ts`, `plugin/src/withIos.ts`, `plugin/tsconfig.json`, `plugin/build/*` — 05-03
- `README.md` — 05-02

<tasks>

<task type="tracer">
  <name>Wire builder-bob and prove `npm run build` emits dual CJS/ESM + .d.ts</name>
  <files>package.json, package-lock.json</files>
  <read_first>
@.planning/research/STACK.md — "builder-bob Configuration" section (lines 53-90) is the source of truth for the config block, output paths, and `files` array.
  </read_first>
  <action>
Install react-native-builder-bob as a devDependency with `npm install --save-dev react-native-builder-bob@^0.43.0` (updates package-lock.json). Then edit package.json to add the `react-native-builder-bob` config block with key `react-native-builder-bob`, value `{ "source": "src", "output": "lib", "targets": [["module", { "esm": true }], ["commonjs", { "esm": true }], "typescript"] }`. Replace the stub `build` script (`"build": "echo 'build deferred to Phase 5'"`) with `"build": "bob build"`. Leave the `typecheck` script (`tsc --noEmit`) and `test` script unchanged. Do NOT add a `codegenConfig` key — this library has no native code of its own (it wraps react-native-tcp-socket), so no codegen spec exists. The three build targets correspond exactly to BUILD-02 (module=ESM, commonjs=CJS) and TYPE-01 (typescript=.d.ts).
  </action>
  <verify>
    <automated>
npm run build && node -e "const fs=require('fs'); const checks=['lib/module/index.js','lib/commonjs/index.js','lib/typescript/src/index.d.ts']; const ok=checks.every(f=>fs.existsSync(f)); console.log('outputs exist:', checks.map(f=>f+':'+fs.existsSync(f)).join(' ')); if(!ok) process.exit(1);" && npx tsc --noEmit && echo "BUILD_OK"
    </automated>
  </verify>
  <done>
- `react-native-builder-bob@^0.43.0` is in devDependencies and installed
- `react-native-builder-bob` config block present with source=src, output=lib, targets [module, commonjs, typescript]
- `build` script runs `bob build` (not the echo stub)
- `npm run build` exits 0 and produces `lib/module/index.js`, `lib/commonjs/index.js`, `lib/typescript/src/index.d.ts`
- `npx tsc --noEmit` passes with strict mode (zero Node API usage in src — BUILD-01)
</done>
</task>

<task type="auto">
  <name>Add exports field with react-native source condition, files, source, and react-native fields</name>
  <files>package.json</files>
  <read_first>
@.planning/research/STACK.md — "builder-bob Configuration" exports/files template (lines 71-89). Note the override below per BUILD-03.
  </read_first>
  <reversibility rating="costly">
The `exports` field is a published-contract decision: it is easy to change now (local-only, no publish in scope) but becomes a breaking API contract at the first npm publish. Supersedes STACK.md's older nested `import`/`require` exports snippet (lines 71-82) which lacks a `react-native` condition.
  </reversibility>
  <action>
Add an `exports` field to package.json using the flat modern builder-bob shape (not STACK.md's older nested snippet), with the key `exports` and value `{ ".": { "types": "./lib/typescript/src/index.d.ts", "react-native": "./src/index.ts", "import": "./lib/module/index.js", "require": "./lib/commonjs/index.js" } }`. The `react-native` condition (pointing at `./src/index.ts`) is required by BUILD-03 — it lets Metro consume TypeScript source directly; the `types`/`import`/`require` conditions deliver .d.ts + ESM + CJS to tooling. Add a `files` field with value `["lib", "src", "app.plugin.js", "plugin/build"]` (the last two paths are created by plan 05-03; including them now is correct per STACK.md's template). Add a `source` field with value `./src/index.ts` and a `react-native` field with value `./src/index.ts`. Do NOT modify `main`, `module`, or `types` — they are already correct.
  </action>
  <verify>
    <automated>
node -e "const p=require('./package.json'); const e=p.exports&&p.exports['.']; const ok = e && e['react-native']==='./src/index.ts' && e['types']==='./lib/typescript/src/index.d.ts' && e['import']==='./lib/module/index.js' && e['require']==='./lib/commonjs/index.js' && Array.isArray(p.files) && p.files.includes('app.plugin.js') && p.files.includes('plugin/build') && p.source==='./src/index.ts' && p['react-native']==='./src/index.ts'; console.log('exports.react-native:', e&&e['react-native']); console.log('exports.types:', e&&e['types']); console.log('files:', JSON.stringify(p.files)); console.log('source:', p.source); console.log('react-native field:', p['react-native']); if(!ok) process.exit(1);"
    </automated>
  </verify>
  <done>
- `exports["."]` has `react-native` → `./src/index.ts`, `types` → `./lib/typescript/src/index.d.ts`, `import` → `./lib/module/index.js`, `require` → `./lib/commonjs/index.js`
- `files` includes `lib`, `src`, `app.plugin.js`, `plugin/build`
- `source` and `react-native` fields both point at `./src/index.ts`
- `main`/`module`/`types` unchanged
</done>
</task>

<task type="auto">
  <name>Declare peerDependencies and verify minimal runtime dependencies</name>
  <files>package.json</files>
  <action>
Add a `peerDependencies` field with value `{ "react": "*", "react-native": "*", "react-native-tcp-socket": "^6.4.2" }` (BUILD-04 — react, react-native, react-native-tcp-socket per ROADMAP success criterion #4). `react-native-tcp-socket` intentionally appears in BOTH `dependencies` (so autolinking installs and links the native module) AND `peerDependencies` (so consumers are pinned to a compatible version) — this dual-listing is deliberate and must be preserved. Verify the `dependencies` field contains exactly these four keys and no others (BUILD-05): `debug`, `events`, `js-md5`, `react-native-tcp-socket`. Note BUILD-05's requirement text lists three deps but `debug` is a genuine runtime import in src/RouterOSAPI.ts and src/RStream.ts (createDebug), so it must remain a dependency — the four already present are the correct minimal set per STACK.md Recommendation #1. Do NOT add any new runtime dependency and do NOT remove any of the four.
  </action>
  <verify>
    <automated>
node -e "const p=require('./package.json'); const pd=p.peerDependencies||{}; const okPeer = pd.react && pd['react-native'] && pd['react-native-tcp-socket']; const depKeys=Object.keys(p.dependencies).sort(); const expect=['debug','events','js-md5','react-native-tcp-socket'].sort(); const okDeps = JSON.stringify(depKeys)===JSON.stringify(expect); console.log('peerDeps:', JSON.stringify(pd)); console.log('runtime deps:', JSON.stringify(depKeys)); console.log('peerDeps ok:', okPeer); console.log('runtime deps exact:', okDeps); if(!okPeer||!okDeps) process.exit(1);"
    </automated>
  </verify>
  <done>
- `peerDependencies` contains `react`, `react-native`, `react-native-tcp-socket`
- `react-native-tcp-socket` present in both `dependencies` and `peerDependencies` (deliberate dual-listing)
- `dependencies` keys are exactly `debug`, `events`, `js-md5`, `react-native-tcp-socket` — no additions, no removals
- `npm run build` still exits 0 after these edits (package.json remains valid JSON)
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | `npm install` of react-native-builder-bob pulls a build-time dependency into the repo |
| Metro/resolver → `./src/index.ts` | The `react-native` exports condition exposes raw TypeScript source to consumers; no runtime trust crossing, but it widens the shipped surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `react-native-builder-bob` devDependency install | high | mitigate | Pin `^0.43.0`; rely on npm integrity hashes in package-lock.json; no postinstall trust beyond the lockfile |
| T-05-02 | Information disclosure | `react-native` exports condition exposing `./src/index.ts` | low | accept | Source is already shipped in `files`; the source condition is the standard builder-bob/Metro pattern and adds no secret surface |
| T-05-03 | Denial of service | `bob build` / `tsc` build tooling | low | accept | Build-time only; no runtime impact |
| T-05-SC | Tampering | npm install (builder-bob) | high | mitigate | slopcheck + blocking human checkpoint for any [ASSUMED]/[SUS] package; keep T-SC row as the standing supply-chain gate |
</threat_model>

<verification>
Run `npm run build` and confirm `lib/module`, `lib/commonjs`, `lib/typescript` all contain `index.js`/`index.d.ts`. Run `npx tsc --noEmit` and confirm zero errors. Load package.json via `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` and confirm valid JSON with the new `exports`, `files`, `peerDependencies`, `source`, `react-native`, and `react-native-builder-bob` fields.
</verification>

<success_criteria>
1. `npm run build` exits 0 and emits ESM (`lib/module`), CJS (`lib/commonjs`), and `.d.ts` (`lib/typescript`) — BUILD-02, TYPE-01
2. `npx tsc --noEmit` passes with `strict: true` — BUILD-01 (zero Node API usage)
3. `exports["."].react-native` === `./src/index.ts` — BUILD-03
4. `peerDependencies` declares react, react-native, react-native-tcp-socket — BUILD-04
5. `dependencies` is exactly `debug`, `events`, `js-md5`, `react-native-tcp-socket` — BUILD-05
</success_criteria>

<output>
Create `.planning/phases/05-build-types-docs-expo-plugin/05-01-SUMMARY.md` when done
</output>
