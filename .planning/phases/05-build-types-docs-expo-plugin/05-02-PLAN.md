---
phase: 05-build-types-docs-expo-plugin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "README documents installation steps and peer dependency setup (DOCS-01)"
    - "README documents Expo config plugin usage via the `plugins` array in app.json (DOCS-01)"
    - "README contains complete, runnable code examples for connect, login, write, writeStream, stream, keepalive, and close (DOCS-02)"
    - "README states in plain language that Expo Go is NOT supported and a custom dev client is required (DOCS-03)"
  artifacts:
    - README.md
  key_links:
    - "README code examples → real public API in src/index.ts (RouterOSAPI, RStream, RosException, IRosOptions)"
    - "README plugin usage → app.json `plugins: [\"react-native-routeros\"]` → app.plugin.js (created in 05-03)"
    - "README peer dependency setup → react, react-native, react-native-tcp-socket (declared in 05-01)"
---

<objective>
Write a complete README that documents installation, peer dependency setup, Expo config plugin usage, and a full set of code examples covering the library's public API — while clearly stating the Expo Go limitation.

Purpose: The library's public API (built in phases 1-4) has no consumer-facing documentation. The README is the deliverable that makes the library usable.
Output: A README.md whose examples reflect the actual shipped API and whose setup instructions match the final package contract from plan 05-01.
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@src/index.ts  (the exact public export surface — RouterOSAPI, RStream, RosException, IRosOptions, IRosGenericResponse, messages, Channel, Connector, win1252, etc.)
@src/RouterOSAPI.ts  (real method signatures: connect(), write(), writeStream(), stream(), keepaliveBy(), close(), setOptions(), openChannel())
@src/RStream.ts  (real stream events: 'data', 'done', 'trap', 'close'; methods pause(), resume(), stop(), close())
@src/types.ts  (IRosOptions fields: host, user, password, port, timeout, tls, keepalive)
@.planning/ROADMAP.md  (success criteria #5-#7)
@.planning/REQUIREMENTS.md  (DOCS-01..03)
@.planning/research/STACK.md  (peer deps, Expo plugin consumer usage — app.json `plugins: ["react-native-routeros"]`)
</context>

## Artifacts this phase produces

- `README.md` (new) — THIS PLAN
- (package.json + lib/* from 05-01; plugin files from 05-03)

<tasks>

<task type="auto">
  <name>README: installation, peer dependency setup, Expo plugin usage, Expo Go warning</name>
  <files>README.md</files>
  <read_first>
@src/index.ts, @src/types.ts, @.planning/research/STACK.md (Expo Config Plugin consumer usage, lines 126-159), @.planning/ROADMAP.md (success criteria #5, #7)
  </read_first>
  <action>
Create README.md (it does not exist yet). Write the following sections, all in plain markdown with no emojis:

1. **Title + one-paragraph description** — "react-native-routeros: a React Native port of node-routeros that talks to MikroTik RouterOS devices over the RouterOS API protocol (raw TCP), with the same developer experience as node-routeros."

2. **Installation** — `npm install react-native-routeros` plus a note that the three peer dependencies must be installed/available: `react`, `react-native`, and `react-native-tcp-socket` (run `npm install react-native-tcp-socket` explicitly for Bare RN, since it is the native socket module). Document that on iOS a `pod install` is required after adding a native module.

3. **Peer dependency setup** — a table or list naming react, react-native, react-native-tcp-socket and stating react-native-tcp-socket is the native transport that must be autolinked.

4. **Expo config plugin usage** — instruct consumers to add `"plugins": ["react-native-routeros"]` to the `expo.plugins` array in app.json, then run `npx expo prebuild` to regenerate the native project. State that the plugin auto-links react-native-tcp-socket and sets the Android INTERNET permission and cleartextTraffic for plain-TCP access (this matches what plan 05-03 implements).

5. **Expo Go warning (DOCS-03)** — a clearly-worded statement that Expo Go is NOT supported and cannot be used: the library requires the react-native-tcp-socket native module, which Expo Go does not bundle; a custom dev client (via `npx expo prebuild` or EAS Build) is required. Phrase it so a reader cannot miss it (e.g. a dedicated "Expo Go is not supported" heading with bold text).

Do NOT invent API details — every claim in the installation/peer-dependency sections must match src/types.ts and the package contract from 05-01.
  </action>
  <verify>
    <automated>
node -e "const fs=require('fs'); const t=fs.readFileSync('README.md','utf8'); const req=['react-native-routeros','react-native-tcp-socket','peerDependencies','plugins','expo prebuild','custom dev client','Expo Go']; const missing=req.filter(s=>!t.includes(s)); console.log('missing terms:', missing.length?missing:'none'); if(missing.length) process.exit(1); const go=t.includes('Expo Go'); const not=t.includes('not supported')||t.includes('NOT supported')||t.includes('Not supported'); console.log('Expo Go mentioned:', go); console.log('unsupported stated:', not); if(!go||!not) process.exit(1);"
    </automated>
  </verify>
  <done>
- README.md exists with title, description, installation, peer dependency setup, Expo plugin usage, and a dedicated Expo Go unsupported statement
- Every term above is present; Expo Go unsupported is stated explicitly (DOCS-03)
- Installation/peer-dependency claims match src/types.ts and the 05-01 package contract
</done>
</task>

<task type="auto">
  <name>README: complete code examples for the full command surface</name>
  <files>README.md</files>
  <read_first>
@src/RouterOSAPI.ts (connect/write/writeStream/stream/keepaliveBy/close/setOptions signatures), @src/RStream.ts (data/done/trap/close events + pause/resume/stop), @src/index.ts (named exports to import)
  </read_first>
  <action>
Append an "Usage" section to README.md with complete, runnable TypeScript code examples that use the REAL public API. Use `import { RouterOSAPI, RStream, RosException } from 'react-native-routeros'` and cite the exact signatures:

- **Connect + login**: construct `new RouterOSAPI({ host: '192.168.88.1', user: 'admin', password: 'password' })` then `await api.connect()`. Explain that `connect()` performs the MD5 challenge-response login automatically (login is internal — there is no separate public login method), so the "connect" and "login" examples are one and the same; show `await api.connect()` resolving to the instance on success.
- **write**: `const addresses = await api.write('/ip/address/print')` — resolves to an array of parsed response objects on `!done`, rejects with a `RosException` on `!trap`.
- **writeStream**: `const s = api.writeStream('/interface/print', ['=type=ether'])` returning an `RStream`; subscribe to `'data'` (each sentence), `'done'`, `'trap'`, and `'close'` events.
- **stream**: `const s = api.stream('/ip/address/listen')` for continuous endpoints, and show the optional callback form `api.stream('/tool/torch', ['=interface=ether1'], (err, packet, stream) => { ... })`; mention `pause()`, `resume()`, `stop()` on the returned RStream.
- **keepalive**: show `api.keepaliveBy('#')` and the constructor alternative `keepalive: true` in IRosOptions (which triggers keepalive automatically on connect).
- **close**: `await api.close()`; note the instance can reconnect via `api.setOptions({...})` then `api.connect()`.
- **Events**: show `api.on('close', ...)`, `api.on('error', ...)`, `api.on('fatal', ...)` for lifecycle/reconnection awareness.

Each example must match the signatures read from src/RouterOSAPI.ts and src/RStream.ts — do not invent parameters, return types, or event names. Use `try/catch` around connect/write to demonstrate `RosException` handling.
  </action>
  <verify>
    <automated>
node -e "const fs=require('fs'); const t=fs.readFileSync('README.md','utf8'); const req=['new RouterOSAPI','connect()','write(','writeStream(','stream(','keepaliveBy(','close()','RouterOSAPI','RStream','RosException','setOptions']; const missing=req.filter(s=>!t.includes(s)); console.log('missing API terms:', missing.length?missing:'none'); if(missing.length) process.exit(1);"
    </automated>
  </verify>
  <done>
- README "Usage" section contains examples for connect+login, write, writeStream, stream, keepalive, close, and lifecycle events
- Every example imports `RouterOSAPI`, `RStream`, `RosException` and uses the real method signatures from src
- `RosException` handling is demonstrated (try/catch around connect/write)
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes documentation only — no runtime code, no dependency installs, no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information disclosure | README example config (host/user/password literals) | low | accept | Examples use placeholder credentials; README adds a note to never hardcode real credentials in production |
| T-05-05 | Security misconfiguration | README plain-TCP (port 8728) examples | low | mitigate | README states TLS on port 8729 is recommended for non-local-network access and that plain TCP is cleartext; the docs must not imply plaintext is safe beyond trusted LANs |

## Notes

No npm installs and no runtime code are touched in this plan, so there is no supply-chain row (T-05-SC) here. The cleartext-traffic tradeoff is documented, not configured, in this plan — the actual Android manifest configuration belongs to plan 05-03's threat model.
</threat_model>

<verification>
Read the final README.md and confirm: (1) all 7 code examples are present and syntactically valid TypeScript against the real signatures, (2) installation + peer deps + plugin usage + Expo Go warning are all present, (3) no emojis and no invented API surface.
</verification>

<success_criteria>
1. README documents installation and peer dependency setup (DOCS-01)
2. README documents Expo config plugin usage in app.json (DOCS-01)
3. README has complete examples for connect, login, write, writeStream, stream, keepalive, close (DOCS-02)
4. README explicitly states Expo Go is not supported and requires a custom dev client (DOCS-03)
</success_criteria>

<output>
Create `.planning/phases/05-build-types-docs-expo-plugin/05-02-SUMMARY.md` when done
</output>
