---
phase: 07-test-package-on-real-routeros-v6-and-v7-routers
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - test/integration/write-read.int.ts
  - test/integration/writeStream.int.ts
  - test/integration/stream.int.ts
  - test/integration/keepalive.int.ts
autonomous: true
requirements: []   # phase_req_ids is TBD/null; informational v1 IDs tagged below

must_haves:
  truths:
    - "`write()` returns parsed arrays for `/system/identity/print`, `/system/resource/print`, `/interface/print`, `/ip/address/print`, and `/user/print` on BOTH routers."
    - "`writeStream()` emits `data`, `done`, and `close` on a finite command, and emits `trap` on an invalid command."
    - "`stream()` emits `data` from a continuous endpoint and honors `pause()`/`resume()`/`stop()` without repeated empty bursts."
    - "`keepaliveBy('#')` / `keepalive: true` keeps the session alive past the RouterOS idle timeout, with no `error` event."
    - "Every spec skips cleanly (exit 0) when its device is unreachable or env is unset; `npm test` (unit) stays green."
  artifacts:
    - test/integration/write-read.int.ts
    - test/integration/writeStream.int.ts
    - test/integration/stream.int.ts
    - test/integration/keepalive.int.ts
  key_links:
    - "`helpers/client.ts` `buildClient()` + reachability `beforeAll` (from 07-01) → each spec's connection lifecycle — breakage = offline routers turn the suite red."
    - "`helpers/version.ts` `detectVersion()` (from 07-01) → write-read's `cpu` vs `cpu-load` field assertion — breakage = wrong field asserted → false failure."
    - "`stream()`/`writeStream()` rely on the same `RStream` debounce path — breakage in `pause`/`resume`/`stop` = STRM-02/03/04 regression."
---

<objective>
Expand from the proven tracer to the command-and-streaming surface: read data across stable services via `write()`, exercise `writeStream()` finite streaming and its trap path, exercise `stream()` continuous streaming with pause/resume/stop, and verify `keepaliveBy()` holds the session open. All specs reuse the 07-01 harness (client + version helpers) and run against both v6 and v7, skipping when offline.

Purpose: Prove every Phase-3 public feature (write/writeStream/stream/keepalive) against real router bytes, filling out the command layer the tracer only touched with a single `write`.

Output: `write-read.int.ts`, `writeStream.int.ts`, `stream.int.ts`, `keepalive.int.ts`.

Informational traceability: exercises CMDS-01 (write), CMDS-02 (writeStream), STRM-01 (stream), STRM-02 (pause/resume), STRM-03 (stop), STRM-04 (empty-data debounce), CONN-06 (keepalive) — tagged for reference only.
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-RESEARCH.md
@src/RouterOSAPI.ts
@src/RStream.ts
@docs/EXAMPLES.md
@test/integration/helpers/client.ts
@test/integration/helpers/version.ts
</context>

<tasks>

<task type="auto">
  <name>write() service reads (v6+v7) + writeStream() finite streaming and trap</name>
  <files>test/integration/write-read.int.ts, test/integration/writeStream.int.ts</files>
  <read_first>
    - 07-RESEARCH.md — Code Examples "Reading other services" (stable across v6/v7), Validation Architecture Test Map, Anti-Patterns (fetch-all + client-side filter, not `?name=value` query words).
    - src/RouterOSAPI.ts — `write()` signature/return type; `writeStream()` returns `RStream` emitting `data`/`done`/`trap`/`close`.
    - src/RStream.ts — event names and debounce behavior.
    - docs/EXAMPLES.md — the 12-scenario feature list (scenario source).
    - test/integration/helpers/client.ts + version.ts — `buildClient()`, `reachable()`, `detectVersion()`.
  </read_first>
  <action>
    File test/integration/write-read.int.ts: for each device (v6/v7 via `buildClient` + `reachable` skip), connect once in `beforeAll` and `write()` each of `/system/identity/print`, `/system/resource/print`, `/interface/print`, `/ip/address/print`, `/user/print`. Assert each resolves to an array; assert row[0] has `.name` (identity), `.version`/`.board-name` (resource), `.name`/`.type` (interface), `.address` (ip), `.name`/`.group` (user). Use `detectVersion(api)` to branch the CPU field assertion: v6 asserts presence of `cpu`, v7 asserts presence of `cpu-load` (A2 — assert field presence, not a numeric value). `afterAll` closes. Fetch-all-then-assert, never `?name=` query-word filtering.

    File test/integration/writeStream.int.ts: connect; `const s = api.writeStream(['/interface/print'])`; attach `data`/`done`/`close`/`trap` listeners and collect sentences; assert `data` fired with at least one sentence, then `done` fired, then `close`. Second test: `api.writeStream(['/definitely/not/a/command'])` emits `trap` (CMDS-02 error path) — assert the trap carries a message. `afterAll` closes.
  </action>
  <verify>
    <automated>npx jest -c jest.integration.config.js write-read.int.ts writeStream.int.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `write-read.int.ts` asserts non-empty parsed arrays for all 5 services on both routers; the resource field assertion branches on `detectVersion` (v6 `cpu` vs v7 `cpu-load`).
    - `writeStream.int.ts` observes `data` then `done` then `close` on `/interface/print`, and `trap` on the invalid command.
    - Both files report SKIP (exit 0) when a device is unreachable or env unset.
    - `npm test` (unit) remains green.
  </acceptance_criteria>
  <done>`write()` is proven against five stable services and `writeStream()` is proven for both the happy and trap paths, on v6 and v7.</done>
</task>

<task type="auto">
  <name>stream() continuous streaming (pause/resume/stop) + keepalive session hold</name>
  <files>test/integration/stream.int.ts, test/integration/keepalive.int.ts</files>
  <read_first>
    - 07-RESEARCH.md — Architecture Patterns (stream `/interface/listen` flow), Don't Hand-Roll (RStream/Transmitter reused), Validation Architecture Test Map (STRM-01..04).
    - src/RStream.ts — `pause()`, `resume()`, `stop()`, empty-data debounce.
    - src/RouterOSAPI.ts — `stream()` signature; `keepaliveBy()` implementation; `keepalive` constructor flag.
    - docs/EXAMPLES.md — stream + keepalive scenarios.
  </read_first>
  <action>
    File test/integration/stream.int.ts: connect; open a bounded continuous stream — prefer `api.stream(['/interface/monitor-traffic', '=interface=<first-interface-from-/interface/print>', '=once='])`; if that endpoint `!trap`s on a device, `test.skip` that device with the trap text recorded. Assert `data` events arrive; call `pause()`, assert no new `data` during a short window; `resume()`, assert `data` resumes; `stop()`, assert the stream stops emitting and no further `data`/`close` spam. Assert no repeated empty-data bursts (STRM-04): each emitted packet is non-empty or debounced.

    File test/integration/keepalive.int.ts: connect with `{ ...buildClient, keepalive: true }` (constructor flag) AND separately verify `api.keepaliveBy('#')` after a plain connect; wait past `(timeout / 2)` seconds, then assert a fresh `write('/system/identity/print')` still resolves (session not timed out) and no `error` event fired on the api instance. `afterAll` closes and clears any keepalive timers.
  </action>
  <verify>
    <automated>npx jest -c jest.integration.config.js stream.int.ts keepalive.int.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `stream.int.ts` observes `data`, honors `pause`/`resume`/`stop`, and sees no repeated empty bursts (skips if the monitor-traffic endpoint traps on a device).
    - `keepalive.int.ts` shows the session alive past `timeout/2` via both `keepalive: true` and `keepaliveBy('#')`, with no `error` event.
    - Both files report SKIP (exit 0) when a device is unreachable or env unset.
    - `npm test` (unit) remains green.
  </acceptance_criteria>
  <done>Continuous streaming (STRM-01..04) and keepalive session hold (CONN-06) are proven against real router bytes on v6 and v7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → lab routers (TCP/TLS/custom port) | `stream()`/`keepalive` leave open channels; must not leak sockets or leave sessions alive |
| git repo ↔ real credentials | unchanged from 07-01 (`.env.test` gitignored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Information Disclosure (Sniffing) | plain-TCP command payloads | medium | accept | Lab-scope cleartext API; TLS suite (07-03) covers the encrypted path |
| T-07-05 | DoS (resource leak) | `stream()`/`keepalive` open channels + timers | medium | mitigate | Always `stop()`/`close()` in `afterAll`; keepalive timers cleared on teardown; sessions never left open |
| T-07-03 | Tampering / DoS | commands to a misconfigured host | high | mitigate | env-driven hosts; `beforeAll` reachability skip; read-only commands for this plan (no destructive CRUD here) |
| T-07-SC | Tampering | npm installs | low | accept | Zero new packages |
</threat_model>

<verification>
- `npx jest -c jest.integration.config.js write-read.int.ts writeStream.int.ts stream.int.ts keepalive.int.ts` green against both routers (or clean SKIP).
- `npm test` (unit) green.
</verification>

<success_criteria>
- All four command-surface specs exist and reuse the 07-01 helpers; they pass on both live routers and skip cleanly offline.
- Unit suite untouched and green.
</success_criteria>

<output>
Create `.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 07-02)

- `test/integration/write-read.int.ts` — `write()` reads across identity/resource/interface/ip/user with version-aware `cpu`/`cpu-load` assertion.
- `test/integration/writeStream.int.ts` — `writeStream()` `data`/`done`/`close` + `trap` path.
- `test/integration/stream.int.ts` — `stream()` continuous with `pause`/`resume`/`stop` + empty-burst guard.
- `test/integration/keepalive.int.ts` — `keepaliveBy('#')` + `keepalive: true` session hold.
