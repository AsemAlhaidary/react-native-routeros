---
phase: 07-test-package-on-real-routeros-v6-and-v7-routers
plan: "03"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - test/integration/recipes/v6.ts
  - test/integration/recipes/v7.ts
  - test/integration/helpers/crud.ts
  - test/integration/helpers/version.ts
  - test/integration/usermanager-crud.int.ts
  - test/integration/tls.int.ts
  - test/integration/error-handling.int.ts
autonomous: true
requirements: []   # phase_req_ids is TBD/null; informational v1 IDs tagged below

must_haves:
  truths:
    - "User Manager CRUD (profile/user/link/router) completes on v6 via `/tool user-manager` and on v7 via `/user-manager`, using the correct per-version paths and fields."
    - "Every created entity carries a unique `gsd-itest-` prefix and is removed after the test, including on failure (afterAll sweep), so no garbage accumulates on the lab routers."
    - "TLS `connect()` to port 8729 succeeds against the self-signed cert on the v6 router."
    - "An invalid command rejects with a plain `Error` carrying the `!trap` message; an unreachable port rejects with `SOCKTMOUT`/`ECONNREFUSED`."
    - "The v7 `!empty` reply is detected and recorded as a known-gap finding, not allowed to fail the suite."
    - "The RN connect-flow divergence (synchronous `onConnect()`) is recorded as a finding; NO library code is patched."
  artifacts:
    - test/integration/recipes/v6.ts
    - test/integration/recipes/v7.ts
    - test/integration/helpers/crud.ts
    - test/integration/helpers/version.ts
    - test/integration/usermanager-crud.int.ts
    - test/integration/tls.int.ts
    - test/integration/error-handling.int.ts
  key_links:
    - "`helpers/version.ts` `selectRecipe()` (added in this plan, 07-03) → picks `recipes/v6` vs `recipes/v7` — breakage = wrong CLI paths sent to the router, causing `!trap`."
    - "`helpers/crud.ts` create→read→update→read→delete→verify-gone + `sweepByPrefix` — breakage = leftover users/profiles on the lab routers."
    - "`recipes/v6.ts` User Manager paths are ASSUMED (research confidence LOW) — the suite reads back via `print` before asserting, making errors self-diagnosing."
    - "`error-handling.int.ts` `!empty` probe must catch-and-record, not throw — breakage = the whole suite dies on v7.18+."
---

<objective>
Complete the phase by proving robustness, security, and the v6/v7-divergent CRUD surface: per-version User Manager recipes + an idempotent CRUD runner, the User Manager integration test, the TLS (8729) path, and error-handling including the v7 `!empty` gap and the recorded RN connect-flow divergence.

Purpose: Cover the phase goal's remaining, most divergent requirements — "v6 vs v7 differ" (User Manager), "TLS", and "error handling" — with self-diagnosing, cleanup-safe tests that report known gaps instead of failing.

Output: `recipes/v6.ts`, `recipes/v7.ts`, `helpers/crud.ts`, `usermanager-crud.int.ts`, `tls.int.ts`, `error-handling.int.ts`.

Informational traceability: exercises CONN-02 (TLS), ERR-01 (`!trap`), ERR-02 (`!fatal` note), ERR-03 (socket errors) — tagged for reference only.

<assumption_delta_decision>
Pluralization signal detected (v6+v7 version matrix). Decision: **add-alongside** — per-router-version recipe modules (`recipes/v6.ts`, `recipes/v7.ts`) keyed by a `detectVersion()` probe (`/system/resource/print` → `.version`), because the v6 (`/tool user-manager customer|user|profile`) and v7 (`/user-manager user|profile|user-profile|router`) command sets genuinely differ. No library code is promoted or changed; this is test-side only. Informational, non-blocking.
</assumption_delta_decision>
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-RESEARCH.md
@src/RouterOSAPI.ts
@src/Channel.ts
@src/Connector.ts
@src/RosException.ts
@test/integration/helpers/version.ts
@test/integration/helpers/client.ts
</context>

<tasks>

<task type="auto">
  <name>Per-version User Manager recipes (v6 /tool user-manager vs v7 /user-manager) + idempotent CRUD runner</name>
  <files>test/integration/recipes/v6.ts, test/integration/recipes/v7.ts, test/integration/helpers/crud.ts, test/integration/helpers/version.ts</files>
  <read_first>
    - 07-RESEARCH.md — Pattern 3 (recipe shape), Pattern 4 (idempotent CRUD + prefix cleanup), Code Examples "v7 User Manager command shapes" (CITED), Assumptions A1 (v6 ASSUMED), Pitfall 5 (leftover garbage), Pitfall 6 (User Manager not enabled).
    - src/RouterOSAPI.ts — `write(path, ...wordArray)` sentence-array convention (`.id=`, `=name=`, `=validity=`, `=password=`, `=group=`, `=user=`, `=profile=`).
    - test/integration/helpers/version.ts — `selectRecipe()` contract the recipes satisfy.
  </read_first>
  <action>
    File test/integration/recipes/v7.ts: export `userManager` with CITED v7 paths/fields — `enable: ['/user-manager/set', '=enabled=yes']`; `profileAdd(name)` → `['/user-manager/profile/add', \`=name=${name}\`, '=validity=1d']`; `profileRead()` → `['/user-manager/profile/print']`; `profileSet(id, validity)` → `['/user-manager/profile/set', \`.id=${id}\`, \`=validity=${validity}\`]`; `profileDel(id)` → `['/user-manager/profile/remove', \`.id=${id}\`]`; `userAdd(name, pw)` → `['/user-manager/user/add', \`=name=${name}\`, \`=password=${pw}\`, '=group=default']`; `userRead()` → `['/user-manager/user/print']`; `userSet(id, pw)` → `['/user-manager/user/set', \`.id=${id}\`, \`=password=${pw}\`]`; `userDel(id)` → `['/user-manager/user/remove', \`.id=${id}\`]`; `linkAdd(user, profile)` → `['/user-manager/user-profile/add', \`=user=${user}\`, \`=profile=${profile}\`]`; `routerAdd(name)` → `['/user-manager/router/add', \`=name=${name}\`, '=address=127.0.0.1', '=shared-secret=gsdtest']`. Also export `fieldForName = 'name'` (v7 uses `name`).

    File test/integration/recipes/v6.ts: export `userManager` with the ASSUMED v6 paths/fields (comment each as ASSUMED, to be confirmed against live v6) — `enable: ['/tool/user-manager/set', '=enabled=yes']`; `profileAdd(name)` → `['/tool/user-manager/profile/add', \`=name=${name}\`, '=validity=1d']`; `profileRead()` → `['/tool/user-manager/profile/print']`; `profileSet(id, validity)` → `['/tool/user-manager/profile/set', \`.id=${id}\`, \`=validity=${validity}\`]`; `profileDel(id)` → `['/tool/user-manager/profile/remove', \`.id=${id}\`]`; `userAdd(name, pw)` → `['/tool/user-manager/user/add', \`=username=${name}\`, \`=password=${pw}\`]`; `userRead()` → `['/tool/user-manager/user/print']`; `userSet(id, pw)` → `['/tool/user-manager/user/set', \`.id=${id}\`, \`=password=${pw}\`]`; `userDel(id)` → `['/tool/user-manager/user/remove', \`.id=${id}\`]`; `linkAdd`/`routerAdd` exported as stubs that throw `'v6 link/router not yet confirmed — record finding'`. Export `fieldForName = 'username'` (v6 uses `username`, ASSUMED). The read-back-assert pattern makes any wrong field self-diagnosing.

    File test/integration/helpers/crud.ts: export `runCrud(api, recipe, entityKind, { add, read, set, del, fieldForName, prefix })` executing create→read(assert present by prefix)→update→read(assert changed)→delete→read(assert gone). Export `makeName(prefix)` → `${prefix}-${random4}` using `process.env.ROUTEROS_PREFIX` defaulting to `gsd-itest`. Export `sweepByPrefix(api, recipe, read, del, prefix)` fetching all, filtering rows whose `name`/`username` starts with the prefix, removing each by `.id`. Idempotent; safe to call in `afterAll`/`finally`.

    Edit test/integration/helpers/version.ts (created in 07-01 with `detectVersion` only): add `export async function selectRecipe(api)` that calls `detectVersion(api)` and returns the `v6` or `v7` recipe module via `await import('../recipes/v6')` / `await import('../recipes/v7')`. The recipe modules are created in this same task, so `npx tsc --noEmit` resolves them — no cross-wave forward reference.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `recipes/v7.ts` `userManager` paths match the CITED v7 `/user-manager` tree (`user`, `profile`, `user-profile`, `router`).
    - `recipes/v6.ts` `userManager` paths use the ASSUMED v6 `/tool user-manager` tree with `username`/`customer`-style fields, each commented ASSUMED.
    - `helpers/crud.ts` `runCrud` + `sweepByPrefix` type-check and use the unique `ROUTEROS_PREFIX` for all created entities.
    - `helpers/version.ts` `selectRecipe(api)` returns the v6 recipe for `major === 6` and the v7 recipe otherwise, and type-checks.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Recipes and CRUD helper are test-scope; any wrong v6 field is fixed by editing the recipe module, with no impact on the library's published contract.</reversibility>
  <done>Per-version User Manager recipes (v6 ASSUMED, v7 CITED) and an idempotent, prefix-cleanup CRUD runner are committed and type-checked.</done>
</task>

<task type="auto">
  <name>User Manager CRUD integration test (v6 + v7, cleanup-safe)</name>
  <files>test/integration/usermanager-crud.int.ts</files>
  <read_first>
    - 07-RESEARCH.md — Pattern 4 (idempotent CRUD), Pitfall 5 (leftover garbage), Pitfall 6 (User Manager not enabled → skip), State of the Art (v6 vs v7 User Manager table).
    - test/integration/recipes/v6.ts + recipes/v7.ts — the recipe contract this test consumes.
    - test/integration/helpers/crud.ts — `runCrud`/`sweepByPrefix`/`makeName`.
    - test/integration/helpers/version.ts — `selectRecipe`.
  </read_first>
  <action>
    File test/integration/usermanager-crud.int.ts: for each device, `selectRecipe(api)` picks the recipe; `beforeAll` attempts `recipe.userManager.enable` (may `!trap` on unlicensed devices → `test.skip` the device describe with the trap text). Tests use `runCrud` for `profile` (add→read→set validity→read→del), `user` (add→read→set password→read→del), `link` (user-profile add → read → remove — v7 only; v6 skips via the recipe's stub), `router` (add→read→remove — v7 only). `afterAll` runs `sweepByPrefix` for profiles and users, then `close()`. All entity names use `makeName(prefix)`; the prefix comes from `ROUTEROS_PREFIX`. Every assertion on `read` filters fetched rows by the unique name (fetch-all-then-filter, never `?name=` query words).
  </action>
  <verify>
    <automated>npx jest -c jest.integration.config.js usermanager-crud.int.ts</automated>
  </verify>
  <acceptance_criteria>
    - `usermanager-crud.int.ts` completes profile/user CRUD on v6 and v7 with correct per-version paths (skips link/router on v6 via the recipe stub).
    - After the run, `sweepByPrefix` removes every `gsd-itest-*` entity — verified by a final `print` returning zero prefix matches.
    - Device describe skips (exit 0) if User Manager is unavailable (`enable` traps) or the router is unreachable.
  </acceptance_criteria>
  <done>User Manager CRUD (create/read/update/delete + verify-gone) is proven on both versions with idempotent cleanup, leaving no garbage on the lab routers.</done>
</task>

<task type="auto">
  <name>TLS (8729) + error-handling (!trap, SOCKTMOUT) + v7 !empty gap detection</name>
  <files>test/integration/tls.int.ts, test/integration/error-handling.int.ts</files>
  <read_first>
    - 07-RESEARCH.md — Security Domain V6 (crypto, `rejectUnauthorized:false` test-scope), Pitfall 2 (`!empty` gap, `Channel.processPacket` no `!empty` case), Common Pitfalls (SOCKTMOUT), Validation Architecture Test Map (TLS + error handling rows).
    - src/Channel.ts — `processPacket` cases (`!re`/`!done` only; `!empty` hits `default` → `UNKNOWNREPLY`).
    - src/RosException.ts — `errno` values (`SOCKTMOUT`, `ECONNREFUSED`, `CANTLOGIN`).
    - src/Connector.ts — `onTimeout()` emits `SOCKTMOUT`; `onError()` wraps as `RosException`.
    - test/integration/helpers/client.ts — `buildClient`, `reachable`.
  </read_first>
  <action>
    File test/integration/tls.int.ts: connect to the v6 router over TLS — `new RouterOSAPI({ host: ROUTEROS_V6_HOST, port: ROUTEROS_V6_TLS_PORT || 8729, user, password, tls: {} })` (empty `tls:{}` enables TLS defaults; self-signed accepted by the shim). Assert `connect()` resolves, then `write('/system/identity/print')` resolves, then `close()`. `beforeAll` reachability probe on 8729; skip the device describe if 8729 is unreachable (recorded reason).

    File test/integration/error-handling.int.ts: (1) invalid command — `api.write('/definitely/not/a/command')` rejects with a plain `Error` (NOT `RosException`) whose `.message` is the router's `!trap` text; (2) unreachable port — `new RouterOSAPI({ host: ROUTEROS_V6_HOST, port: 1, user, password, timeout: 3 }).connect()` rejects with `RosException` whose `errno` is `SOCKTMOUT` or `ECONNREFUSED`; (3) v7 `!empty` probe — on the v7 device only, issue a command expected to return no data (e.g. a `set` matching nothing, or `print` of an empty list); wrap in try/catch: if it throws `UNKNOWNREPLY` (or a `!empty`-derived error), record a finding log and mark a dedicated "known-gap" assertion pass — do NOT let it fail the suite. Also assert `connect()` with `{ timeout: 1 }` to a blackhole host resolves-or-rejects without an unhandled rejection (ERR-03).
  </action>
  <verify>
    <automated>npx jest -c jest.integration.config.js tls.int.ts error-handling.int.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tls.int.ts` connects on 8729 (self-signed accepted) and completes a `write` + `close`; skips when 8729 is unreachable.
    - `error-handling.int.ts` shows `!trap` → plain `Error`, unreachable port → `SOCKTMOUT`/`ECONNREFUSED`, and the v7 `!empty` probe records a finding without failing.
    - `npx jest -c jest.integration.config.js` (full suite) exits 0 with the known-gap findings logged, never a hard failure on the `!empty` case.
  </acceptance_criteria>
  <reversibility rating="reversible">TLS/error-handling specs are read-only test code; the `!empty` probe is a catch-and-record, not a library patch.</reversibility>
  <done>TLS (8729) and the full error surface (`!trap`, `SOCKTMOUT`/`ECONNREFUSED`, `!empty`) are proven or recorded as known-gap findings against the live routers, with no library code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → lab routers (TCP 8728 / TLS 8729 / custom 8175) | plaintext admin creds (TCP) and self-signed TLS (lab) cross the LAN boundary |
| test harness → lab router state | destructive CRUD writes to User Manager — must be prefix-guarded and cleaned |
| git repo ↔ real credentials | `.env.test` must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering / DoS | User Manager CRUD against a live (possibly non-lab) router | high | mitigate | Unique `ROUTEROS_PREFIX` (`gsd-itest-`) on every created entity; `runCrud` deletes each entity; `afterAll` `sweepByPrefix` removes leftovers; env-driven hosts + reachability skip; `.env.test.example` documents lab-only requirement |
| T-07-06 | Tampering / DoS | `enable` toggling User Manager on a shared device | medium | mitigate | `enable` is attempted in `beforeAll` only and skipped-with-reason if it `!trap`s (unlicensed/disabled); never silently mutates unrelated config |
| T-07-04 | Crypto misuse | TLS `rejectUnauthorized: false` in the shim | medium | mitigate | Test-scope only, never shipped as library behavior; TLS suite proves the encrypted 8729 path works |
| T-07-02 | Information Disclosure (Sniffing) | plain-TCP creds/commands | medium | accept | Lab-scope cleartext API; documented encrypted path via the TLS suite |
| T-07-01 | Information Disclosure | credentials in git | high | mitigate | `.env.test` gitignored; recipes/specs contain zero real creds |
| T-07-SC | Tampering | npm installs | low | accept | Zero new packages |
</threat_model>

<verification>
- `npx jest -c jest.integration.config.js` (full suite) green against both routers with the `!empty` finding recorded (not fatal).
- `npm test` (unit) green.
- `npx tsc --noEmit` green.
- No `gsd-itest-*` entities remain on either router after a run (cleanup verified by final `print`).
</verification>

<success_criteria>
- Per-version recipes + CRUD runner + User Manager test exist and prove create/read/update/delete on v6 and v7 with cleanup.
- TLS (8729) and error handling (`!trap`, socket errors, `!empty` finding) are proven/recorded.
- The RN connect-flow divergence is recorded as a finding; no library code is patched.
</success_criteria>

<output>
Create `.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 07-03)

- `test/integration/recipes/v7.ts` — CITED v7 `/user-manager` command arrays.
- `test/integration/recipes/v6.ts` — ASSUMED v6 `/tool user-manager` command arrays (self-diagnosing).
- `test/integration/helpers/crud.ts` — `runCrud` / `makeName` / `sweepByPrefix` idempotent CRUD runner.
- `test/integration/helpers/version.ts` (edit) — adds `selectRecipe()` lazy-loading `recipes/v6`/`recipes/v7`.
- `test/integration/usermanager-crud.int.ts` — v6/v7 User Manager CRUD (profile/user/link/router) with cleanup.
- `test/integration/tls.int.ts` — TLS 8729 self-signed connect/write/close.
- `test/integration/error-handling.int.ts` — `!trap`, `SOCKTMOUT`/`ECONNREFUSED`, v7 `!empty` finding.
