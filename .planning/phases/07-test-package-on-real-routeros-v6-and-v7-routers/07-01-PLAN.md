---
phase: 07-test-package-on-real-routeros-v6-and-v7-routers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.integration.config.js
  - test/integration/setup.ts
  - test/integration/mocks/react-native.ts
  - test/integration/mocks/react-native-tcp-socket.ts
  - test/integration/helpers/client.ts
  - test/integration/helpers/version.ts
  - test/integration/connect-login.int.ts
  - .env.test.example
  - .gitignore
  - package.json
  - COVERAGE.md
autonomous: true
requirements: []   # phase_req_ids is TBD/null — no mapped requirement IDs; informational v1 IDs tagged below
user_setup:
  - service: mikrotik_lab_routers
    why: "Integration tests connect to live RouterOS v6/v7 devices over the LAN; the agent cannot know real credentials or the exact v7 API port."
    env_vars:
      - name: ROUTEROS_V6_HOST
        source: "lab v6 router IP (goal: 192.168.187.128)"
      - name: ROUTEROS_V6_PORT
        source: "v6 API port (goal: 8728)"
      - name: ROUTEROS_V7_HOST
        source: "lab v7 router IP (goal: 192.168.187.130)"
      - name: ROUTEROS_V7_PORT
        source: "v7 custom API port (goal: 8175 — CONFIRM this is a custom /ip service API port, not a typo)"
      - name: ROUTEROS_USER
        source: "lab admin username (goal: admin)"
      - name: ROUTEROS_PASSWORD
        source: "lab admin password — never commit; write only to gitignored .env.test"
    dashboard_config:
      - task: "Confirm the v7 service on port 8175 is a RouterOS API service (8728/8729 are closed on the v7 host)."

must_haves:
  truths:
    - "Running `npx jest -c jest.integration.config.js connect-login.int.ts` with routers reachable proves connect → MD5 login → write(`/system/identity/print`) → close works end-to-end against BOTH the v6 and v7 devices."
    - "When ROUTEROS_* env vars are unset or a router is unreachable, the tracer test reports SKIP and exits 0 — never a red run."
    - "The existing unit suite (`npm test`, roots `<rootDir>/src`) stays green and never picks up the integration tests."
    - "No credentials or lab IPs are committed to git; only placeholders appear in `.env.test.example`."
  artifacts:
    - jest.integration.config.js
    - test/integration/setup.ts
    - test/integration/mocks/react-native.ts
    - test/integration/mocks/react-native-tcp-socket.ts
    - test/integration/helpers/client.ts
    - test/integration/helpers/version.ts
    - test/integration/connect-login.int.ts
    - .env.test.example
    - COVERAGE.md
  key_links:
    - "`moduleNameMapper`: `^react-native$` → `test/integration/mocks/react-native.ts`, `^react-native-tcp-socket$` → `test/integration/mocks/react-native-tcp-socket.ts` — the ONLY seam that lets the library load under Node jest; breakage = every test fails to import."
    - "`setup.ts` → `process.loadEnvFile('.env.test')` → `ROUTEROS_V6_HOST/PORT`, `ROUTEROS_V7_HOST/PORT`, `ROUTEROS_USER/PASSWORD` — breakage = tests target null/placeholder hosts."
    - "`helpers/client.ts` reachability probe → `beforeAll` skip — breakage = offline routers turn the suite red."
---

<objective>
Build the minimal integration-test harness (jest config + module-mapper shims + env loading + a connect/login/write/close test) and prove the library's REAL protocol code runs end-to-end against both live routers. This is the tracer slice: one path through every layer (env → RouterOSAPI.connect() → Node net/tls bridge → real TCP handshake → MD5 login → write → close), verified before any expansion.

Purpose: De-risk the phase architecture on the best early-context tokens — if the net-bridge or env gating is wrong, we learn it after one committed harness + one test, not after ten already-committed specs.

Output: `jest.integration.config.js`, the two `moduleNameMapper` shims (`react-native.ts`, `react-native-tcp-socket.ts`), `setup.ts`, `helpers/client.ts`, `helpers/version.ts`, `connect-login.int.ts`, `.env.test.example`, `.gitignore` edit, `package.json` `test:integration` script, and `COVERAGE.md`.

Informational traceability: exercises v1 requirement IDs CONN-01 (plain TCP), CONN-04 (close), CONN-05 (reconnect), AUTH-01/AUTH-02 (v6+v7 MD5 login), AUTH-03 (CANTLOGIN), CMDS-01 (write) — tagged for reference only; this phase has no mapped `phase_req_ids`.
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-RESEARCH.md
@src/RouterOSAPI.ts
@src/transport/SocketAdapter.ts
@src/Connector.ts
@package.json
@.gitignore
@examples/basic-usage.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end connect → login → write(/system/identity/print) → close on BOTH routers via the Node-net bridge</name>
  <files>jest.integration.config.js, test/integration/setup.ts, test/integration/mocks/react-native.ts, test/integration/mocks/react-native-tcp-socket.ts, test/integration/helpers/client.ts, test/integration/connect-login.int.ts, .env.test.example, .gitignore, package.json</files>
  <read_first>
    - 07-RESEARCH.md — Pattern 1 (module-mapper shim), Pattern 2 (env-gated reachability-skip), Code Examples (login flow), Pitfall 4 (jest base config capture).
    - src/transport/SocketAdapter.ts — the exact `TcpSockets.createConnection({port,host,connectTimeout}, cb)` and `TcpSockets.connectTLS(tlsOpts, cb)` call shapes the shim must mirror.
    - src/RouterOSAPI.ts — `connect()` performs internal MD5 login; `write()` returns `Promise<Record<string,any>[]>`; `close()` resolves on full teardown.
    - src/Connector.ts — `onConnect()` is synchronous; the net-bridge masks this (record-only, do NOT patch).
    - package.json — existing `jest.roots` is `["<rootDir>/src"]` (must not capture `test/`).
    - .gitignore — currently lists `node_modules/`, `node-routeros/`, `lib/`, `plugin/build/`.
  </read_first>
  <action>
    Create the harness so the library's real protocol code runs under Node jest against live routers, with NO library source changes.

    File jest.integration.config.js: export a config object with `preset: 'ts-jest'`, `testEnvironment: 'node'`, `rootDir: '.'`, `roots: ['<rootDir>/test/integration']`, `testMatch: ['**/*.int.ts']`, `setupFiles: ['<rootDir>/test/integration/setup.ts']`, `testTimeout: 30000`, and `moduleNameMapper` mapping `'^react-native$'` to `<rootDir>/test/integration/mocks/react-native.ts` and `'^react-native-tcp-socket$'` to `<rootDir>/test/integration/mocks/react-native-tcp-socket.ts`.

    File test/integration/mocks/react-native.ts: export a named `AppState` object whose `addEventListener(_type, _cb)` returns `{ remove() {} }` (no-op subscription). This satisfies the `AppState` import in RouterOSAPI.ts.

    File test/integration/mocks/react-native-tcp-socket.ts: default-export an object with `createConnection(options, callback?)` returning `net.createConnection({ host: options.host, port: options.port }, callback)` and `connectTLS(options, callback?)` returning `tls.connect({ ...rest, rejectUnauthorized: false }, callback)` where `rest` strips `certAlias`/`keyAlias`. Import built-in `net` and `tls`. The `rejectUnauthorized: false` is test-scope only for the lab self-signed cert.

    File test/integration/setup.ts: wrap `process.loadEnvFile('.env.test')` in try/catch (missing file = acceptable defaults). This is a jest `setupFiles` entry, run before each test file.

    File test/integration/helpers/client.ts: export `loadTestEnv()`, `reachable(host: string, port: number, ms = 2000): Promise<boolean>` (raw `net.Socket` probe that resolves false on timeout/error/refused), and `buildClient(cfg: {host, port})` returning `new RouterOSAPI({ host, port, user: process.env.ROUTEROS_USER, password: process.env.ROUTEROS_PASSWORD, timeout: 10 })`. Derive config from env: `ROUTEROS_V6_HOST`, `ROUTEROS_V6_PORT`, `ROUTEROS_V7_HOST`, `ROUTEROS_V7_PORT`, `ROUTEROS_USER`, `ROUTEROS_PASSWORD`.

    File test/integration/connect-login.int.ts: define a `describe` per device (v6 from `ROUTEROS_V6_HOST/PORT`, v7 from `ROUTEROS_V7_HOST/PORT`). In `beforeAll`, if host/port unset or `reachable()` returns false, `test.skip` the whole device describe with a clear reason. Tests, in order: (1) `connect()` resolves to the `RouterOSAPI` instance; (2) `write('/system/identity/print')` resolves to a non-empty array whose first row has a `.name` field; (3) `write('/system/resource/print')` resolves and its first row's `.version` is logged (first assertion per research Open Question 3); (4) `close()` resolves, then reconnect via `setOptions({...same})` + `connect()` succeeds (CONN-05); (5) a `new RouterOSAPI({ host, port, user, password: 'wrong-password' })` connect rejects with a `RosException` whose `errno === 'CANTLOGIN'` (AUTH-03). `afterAll`: `api.close()` if connected. Guard every test so absence of env/device yields skip, not failure.

    File .env.test.example: commit a template with placeholder values and a comment header — `ROUTEROS_V6_HOST=192.168.187.128`, `ROUTEROS_V6_PORT=8728`, `ROUTEROS_V7_HOST=192.168.187.130`, `ROUTEROS_V7_PORT=8175`, `ROUTEROS_USER=admin`, `ROUTEROS_PASSWORD=CHANGE_ME`, `ROUTEROS_PREFIX=gsd-itest`, `ROUTEROS_INTEGRATION=1`. No real secrets.

    Edit .gitignore: append `.env.test` (the real secrets file is never committed).

    Edit package.json: add script `"test:integration": "jest -c jest.integration.config.js"`. Do not alter the existing `"test"` script or `jest.roots`.
  </action>
  <verify>
    <automated>npx jest -c jest.integration.config.js connect-login.int.ts</automated>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest -c jest.integration.config.js connect-login.int.ts --listTests` lists exactly `connect-login.int.ts` (and no `src/` unit tests).
    - With `.env.test` populated and routers reachable, the connect → login → write(`/system/identity/print`) → close test PASSES against both v6 and v7; the reconnect and CANTLOGIN tests PASS.
    - With env unset or routers unreachable, the device describes report SKIP and jest exits 0.
    - `npm test` (unit, roots `<rootDir>/src`) remains green and reports zero integration files.
    - `npx tsc --noEmit` passes (library + new test files type-check).
    - `.env.test` is listed in `.gitignore`; `.env.test.example` contains no real credentials.
  </acceptance_criteria>
  <reversibility rating="reversible">The net-bridge + moduleNameMapper harness and the new npm script are test-scope only and trivially removable; no published library contract is touched (zero `src/` changes).</reversibility>
  <done>The tracer proves the net-bridge harness runs the unmodified protocol code end-to-end: connect/login/write/close (plus reconnect + CANTLOGIN) works against both live routers, and skips cleanly when offline.</done>
</task>

<task type="auto">
  <name>Version probe helper (detectVersion/selectRecipe) + COVERAGE.md declaration</name>
  <files>test/integration/helpers/version.ts, COVERAGE.md</files>
  <read_first>
    - 07-RESEARCH.md — Pattern 3 (per-version recipes), Open Question 3 (version unknown), Code Examples (`/system/resource/print` → `.version`).
    - src/RouterOSAPI.ts — `write()` returns parsed rows; row fields are string-indexed.
  </read_first>
  <action>
    File test/integration/helpers/version.ts: export `async function detectVersion(api): Promise<{ major: 6 | 7; full: string }>` that reads `api.write('/system/resource/print')`, takes `data[0].version`, parses the leading integer into `major` (anything not starting with 6 treated as 7), and returns both. Export `async function selectRecipe(api)` that calls `detectVersion` and returns the `v6` or `v7` recipe module (lazy-import from `../recipes/v6` / `../recipes/v7` — those files land in plan 07-03; import them defensively so plan 07-01 type-checks before they exist by declaring the import inside the function body).

    File COVERAGE.md: write the reasoned declaration verbatim — a single paragraph: "No external API integration: this phase tests the existing RouterOS API integration; it adds no new external API/SDK surface." Add one sentence noting the harness reuses `jest`/`ts-jest` (already devDeps) and Node built-in `net`/`tls`, so zero new packages or SDK integrations are introduced.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `detectVersion(api)` returns `{ major: 6, full: '6.x.y' }` or `{ major: 7, full: '7.x.y' }` from a live router's `/system/resource/print`.
    - `COVERAGE.md` exists and contains the mandated declaration "No external API integration: this phase tests the existing RouterOS API integration; it adds no new external API/SDK surface."
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>A reusable version probe helper and the API-coverage declaration are committed, ready for the Wave-2 expansion specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → lab routers (TCP 8728 / TLS 8729 / custom 8175) | Untrusted-ish network path; plaintext admin creds cross the LAN boundary |
| git repo ↔ real credentials | `.env.test` must never cross into the committed tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `.env.test` vs `.env.test.example` | high | mitigate | `.env.test` gitignored; `.env.test.example` committed with `ROUTEROS_PASSWORD=CHANGE_ME` and comment header; no real IPs/creds in any test file or plan |
| T-07-02 | Information Disclosure (Sniffing) | plain-TCP login on 8728 (MD5 challenge over cleartext) | medium | accept | Inherent to RouterOS cleartext API; accepted for lab scope; TLS suite (07-03) documents the 8729 encrypted path |
| T-07-03 | Tampering / DoS | test commands mutate a misconfigured (non-lab) host | high | mitigate | hosts/ports fully env-driven; unique `ROUTEROS_PREFIX` (`gsd-itest`) + cleanup; lab-subnet guard documented in `.env.test.example` |
| T-07-04 | Crypto misuse | shim `rejectUnauthorized: false` | medium | mitigate | Test-scope only, never shipped as library behavior; library's own `js-md5` login path is exercised unmodified |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (jest/ts-jest/@types/jest already devDeps; net/tls/process.loadEnvFile are Node built-ins) |
</threat_model>

<verification>
- `npx jest -c jest.integration.config.js connect-login.int.ts` green against both routers (or clean SKIP).
- `npm test` (unit) stays green and shows no integration files.
- `npx tsc --noEmit` green.
- `git status` shows `.env.test` untracked-and-ignored; no real secrets staged.
</verification>

<success_criteria>
- Harness config + both module-mapper shims + env setup exist and are wired.
- The tracer test proves connect/login/write/close (plus reconnect + CANTLOGIN) against both live routers, degrading to SKIP when offline.
- Unit suite is untouched and green.
- `COVERAGE.md` and `.env.test.example` are committed; `.env.test` is gitignored.
</success_criteria>

<output>
Create `.planning/phases/07-test-package-on-real-routeros-v6-and-v7-routers/07-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (plan 07-01)

- `jest.integration.config.js` — separate ts-jest config (roots `test/integration`, `testMatch **/*.int.ts`, `moduleNameMapper` for the two mocks, `setupFiles`, `testTimeout: 30000`).
- `test/integration/setup.ts` — `process.loadEnvFile('.env.test')`.
- `test/integration/mocks/react-native.ts` — no-op `AppState` stub.
- `test/integration/mocks/react-native-tcp-socket.ts` — `createConnection`/`connectTLS` bridge to Node `net`/`tls`.
- `test/integration/helpers/client.ts` — `loadTestEnv()`, `reachable()`, `buildClient()`.
- `test/integration/helpers/version.ts` — `detectVersion()`, `selectRecipe()`.
- `test/integration/connect-login.int.ts` — tracer: connect/login/write/close/reconnect/CANTLOGIN against v6+v7.
- `.env.test.example` — committed placeholder template.
- `.gitignore` (edit) — `+.env.test`.
- `package.json` (edit) — `+"test:integration"` script.
- `COVERAGE.md` — no-external-API declaration.
