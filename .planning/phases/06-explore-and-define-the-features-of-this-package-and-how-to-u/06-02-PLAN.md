---
phase: 06-explore-and-define-the-features-of-this-package-and-how-to-u
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - docs/EXAMPLES.md
  - docs/INSTALLATION.md
  - README.md
autonomous: true
requirements: []   # Phase 6 has no mapped requirement IDs (ROADMAP "Requirements: TBD"); verification is via must_haves below.

must_haves:
  truths:
    - "docs/EXAMPLES.md shows real-world scenarios using only the real public API (write, writeStream, stream, keepalive, close, reconnection, TLS, error handling)"
    - "docs/INSTALLATION.md documents both Bare React Native and Expo custom dev client install paths end-to-end"
    - "README.md links to the new documentation and the existing Installation/Usage sections are preserved"
  artifacts:
    - docs/EXAMPLES.md
    - docs/INSTALLATION.md
    - README.md (modified)
  key_links:
    - "docs/EXAMPLES.md examples follow the verified pattern established in examples/basic-usage.ts (06-01)"
    - "docs/INSTALLATION.md commands match package.json peerDependencies and the Expo config plugin in plugin/src/*"
    - "README.md Documentation section links to docs/INSTALLATION.md, docs/API.md, docs/EXAMPLES.md"
---

<objective>
Complete the documentation phase by expanding from the 06-01 tracer into real-world scenario recipes
(`docs/EXAMPLES.md`), a full installation process guide (`docs/INSTALLATION.md`), and README enrichment
linking the deeper guides together.

Purpose: Give a developer the ability to install react-native-routeros on both Bare RN and Expo custom
dev client, then copy working scenario examples (monitoring, configuration writes, streaming, keepalive,
reconnection, TLS) that are guaranteed consistent with the shipped API.
Output: `docs/EXAMPLES.md`, `docs/INSTALLATION.md`, and a modified `README.md` with a Documentation section.

Note: Phase 6 has no mapped requirement IDs (ROADMAP `Requirements: TBD`). Completion is verified
goal-backward via `must_haves` above.
</objective>

<execution_context>
@C:/Users/Asem/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Asem/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Ground truth for the public API (read before writing examples)
@src/RouterOSAPI.ts
@src/RStream.ts
@src/types.ts
@src/Connector.ts
@README.md
@package.json

# Expo config plugin surface (read before writing the installation guide)
@app.plugin.js
@plugin/src/index.ts
@plugin/src/withAndroid.ts
@plugin/src/withIos.ts

# Tracer anchor produced by 06-01 — EXAMPLES.md must follow this verified pattern
@examples/basic-usage.ts
@docs/API.md
</context>

## Artifacts this phase produces

| Artifact | Produced by | Type |
|----------|-------------|------|
| `examples/basic-usage.ts` | 06-01 Task 1 (tracer) | canonical end-to-end example |
| `docs/API.md` | 06-01 Task 2 | feature/API reference |
| `docs/EXAMPLES.md` | 06-02 Task 1 | real-world scenario recipes |
| `docs/INSTALLATION.md` | 06-02 Task 2 | installation process guide |
| `README.md` (modified) | 06-02 Task 3 | Documentation section linking to `docs/` |

These five artifacts are the complete deliverable set for this documentation phase. All are markdown
guides or example snippets grounded in the existing library source; no library code is added.

<tasks>

<task type="auto">
  <name>Real-world scenario recipes grounded in the shipped API</name>
  <files>docs/EXAMPLES.md</files>
  <read_first>
    - examples/basic-usage.ts (tracer anchor — every example must follow its import/connect pattern)
    - src/RouterOSAPI.ts (write, writeStream, stream, keepaliveBy, close signatures and events)
    - src/RStream.ts (data, pause, resume, stop, close; events data/done/trap/close/error)
    - src/types.ts (IRosOptions, TlsRnOptions field names)
    - README.md (existing example terminology to stay consistent with)
  </read_first>
  <action>
    Create docs/EXAMPLES.md with real-world scenario recipes, each consistent with examples/basic-usage.ts
    and using only methods present in src/RouterOSAPI.ts and src/RStream.ts. Cover at minimum: (1) Connect +
    error handling mirroring the tracer (try/catch RosException, read errno + message). (2) Read resources —
    write('/ip/address/print'), write('/interface/print', ['=type=ether']), write('/system/resource/print').
    (3) Write configuration — write('/ip/firewall/filter/add', ['=chain=input', '=action=drop',
    '=comment=block']). (4) Continuous monitoring — stream('/ip/address/listen') with an on('data') listener.
    (5) Torch — stream('/tool/torch', ['=interface=ether1'], callback) using the three-argument
    (err, packet, stream) callback shape declared in RStream. (6) Stream lifecycle — pause(), resume(), and
    stop() (each returns Promise&lt;void&gt;). (7) Concurrent commands — issue multiple write() calls with
    independent tags. (8) keepalive — keepaliveBy('#') and the keepalive: true constructor option.
    (9) Reconnection — listen to 'close'/'fatal'/'error' events, then setOptions(...) followed by connect().
    (10) React Native lifecycle — note the library subscribes to AppState internally and the consumer owns
    reconnection via the 'close' event. (11) TLS — tls: {} (defaults port 8729) and tls: { ca } for a
    self-signed RouterOS CA, with PEM content supplied by the consumer. (12) Error handling — catch
    RosException and look up errno against the messages catalog. Use placeholder credentials throughout and
    a note to use env vars / secure key store. Do not invent methods and do not use Node built-ins.
  </action>
  <acceptance_criteria>
    - docs/EXAMPLES.md contains a scenario for each of: connect+error, read resources, write config, monitoring stream, torch stream, pause/resume/stop, concurrent commands, keepalive, reconnection, TLS, error handling
    - docs/EXAMPLES.md stream callback uses the (err, packet, stream) three-argument form matching RStream
    - docs/EXAMPLES.md every method call (write, writeStream, stream, keepaliveBy, close, setOptions, connect) matches the signature in src/RouterOSAPI.ts
    - docs/EXAMPLES.md does not reference a public login method (login is private)
    - docs/EXAMPLES.md TLS example uses `tls: {}` and `tls: { ca }` per src/Connector.ts and src/types.ts
  </acceptance_criteria>
  <verify>
    <automated>rg -c "write\(|writeStream\(|stream\(|keepaliveBy\(|\.connect\(\)|\.close\(\)|setOptions\(|tls:|pause\(\)|resume\(\)|stop\(\)" docs/EXAMPLES.md</automated>
  </verify>
  <done>
    docs/EXAMPLES.md exists with runnable real-world scenario recipes covering the full command surface,
    all consistent with the tracer example and the real public API, with placeholder credentials and no
    invented methods.
  </done>
</task>

<task type="auto">
  <name>Installation process guide for Bare RN and Expo custom dev client</name>
  <files>docs/INSTALLATION.md</files>
  <read_first>
    - README.md (existing installation, peer deps, and Expo plugin sections to expand)
    - package.json (exact package name, dependencies, peerDependencies: react, react-native, react-native-tcp-socket@^6.4.2)
    - app.plugin.js and plugin/src/index.ts, plugin/src/withAndroid.ts, plugin/src/withIos.ts (config plugin behavior)
  </read_first>
  <action>
    Create docs/INSTALLATION.md documenting the full installation process for both targets. Cover: (1)
    Prerequisites — Node, an existing React Native project, and a MikroTik RouterOS device with the API
    service enabled (RouterOS /ip service set api port=8728). (2) Bare React Native — npm install
    react-native-routeros, then install the peer dependencies explicitly (npm install react react-native
    react-native-tcp-socket), then iOS `cd ios && pod install && cd ..`; note react-native-tcp-socket
    autolinks on RN 0.60+ and is the only native dependency. (3) Expo — npm install react-native-routeros
    react-native-tcp-socket, add "plugins": ["react-native-routeros"] to app.json, run npx expo prebuild,
    then npx expo run:ios / npx expo run:android; state explicitly that Expo Go is NOT supported and a
    custom dev client is required. (4) Config plugin behavior — describe that plugin/src/withAndroid.ts adds
    the android.permission.INTERNET permission and android:usesCleartextTraffic="true" and verifies
    react-native-tcp-socket is installed, while plugin/src/withIos.ts emits a non-fatal warning if the
    module is missing. (5) Verification — confirm the native module is linked and run a minimal
    connect('/system/resource/print') test. Use exact npm package names and peer versions from package.json;
    do not invent commands or claim Expo Go compatibility.
  </action>
  <acceptance_criteria>
    - docs/INSTALLATION.md contains the exact strings `npm install react-native-routeros` and `react-native-tcp-socket`
    - docs/INSTALLATION.md documents the iOS `pod install` step
    - docs/INSTALLATION.md documents the Expo path: `plugins` array with `"react-native-routeros"`, `npx expo prebuild`, and `npx expo run:ios`/`run:android`
    - docs/INSTALLATION.md states Expo Go is NOT supported (custom dev client required)
    - docs/INSTALLATION.md describes the Android INTERNET permission + usesCleartextTraffic and the iOS non-fatal warning, matching plugin/src/withAndroid.ts and withIos.ts
    - docs/INSTALLATION.md peer dependency versions match package.json (react-native-tcp-socket ^6.4.2)
  </acceptance_criteria>
  <verify>
    <automated>rg -c "npm install react-native-routeros|react-native-tcp-socket|pod install|npx expo prebuild|plugins|Expo Go|INTERNET|usesCleartextTraffic" docs/INSTALLATION.md</automated>
  </verify>
  <done>
    docs/INSTALLATION.md exists and documents the end-to-end installation for Bare RN and Expo custom dev
    client, including peer deps, pod install, config plugin behavior, the Expo Go limitation, and
    verification steps — all matching package.json and plugin/src/*.
  </done>
</task>

<task type="auto">
  <name>Link the documentation from README</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing Installation and Usage sections to preserve)
    - docs/INSTALLATION.md, docs/API.md, docs/EXAMPLES.md, examples/basic-usage.ts (artifacts to link)
  </read_first>
  <action>
    Edit README.md (scoped, do not rewrite existing sections) to append a new "Documentation" section after
    the existing Usage content, containing relative markdown links to the deeper guides: docs/INSTALLATION.md
    (installation guide), docs/API.md (full API/feature reference), docs/EXAMPLES.md (real-world scenario
    recipes), and examples/basic-usage.ts (canonical end-to-end example). Preserve the existing Installation,
    Peer Dependencies, Expo Config Plugin, Expo Go, and Usage sections verbatim.
  </action>
  <acceptance_criteria>
    - README.md contains relative links to docs/INSTALLATION.md, docs/API.md, docs/EXAMPLES.md, and examples/basic-usage.ts
    - README.md still contains the existing "## Installation" and "## Usage" headings
    - No existing README section content is removed or rewritten
  </acceptance_criteria>
  <verify>
    <automated>rg -c "docs/INSTALLATION.md|docs/API.md|docs/EXAMPLES.md|examples/basic-usage.ts" README.md</automated>
  </verify>
  <done>
    README.md gains a Documentation section linking all four deeper guides while preserving the existing
    Installation and Usage sections unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → reader | Example code and installation prose must not leak credentials or instruct insecure practice |
| Consumer app → RouterOS device | Plain TCP 8728 is cleartext; TLS 8729 is the encrypted path (documented, not changed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | docs/EXAMPLES.md, docs/INSTALLATION.md | high | mitigate | All examples use placeholder credentials ('admin'/'password') with an explicit note to use env vars / secure key store; no real secrets committed |
| T-06-02 | Spoofing (MITM) | docs/EXAMPLES.md TLS scenario | high | mitigate | Document `tls: { ca }` PEM pinning for the self-signed RouterOS CA and recommend TLS (8729) beyond trusted LANs; never show disabling certificate validation |
| T-06-03 | Information Disclosure | docs/INSTALLATION.md RouterOS prerequisites | medium | mitigate | Document enabling the RouterOS API service without instructing weak-password or disabled-auth RouterOS configuration |

No new dependencies are installed in this documentation phase, so no supply-chain (npm/pip/cargo) install
row applies. Existing runtime deps are unchanged from prior phases.
</threat_model>

<verification>
Phase-level checks for this plan:
- `rg -c "write\(|stream\(|keepaliveBy\(|tls:" docs/EXAMPLES.md` shows the full command surface is demonstrated
- `rg -c "npm install react-native-routeros|react-native-tcp-socket|npx expo prebuild|Expo Go" docs/INSTALLATION.md` shows both install paths are documented
- `rg -c "docs/INSTALLATION.md|docs/API.md|docs/EXAMPLES.md|examples/basic-usage.ts" README.md` shows the Documentation section links are present
</verification>

<success_criteria>
- docs/EXAMPLES.md exists with source-grounded scenario recipes covering the full public API surface
- docs/INSTALLATION.md exists covering Bare RN and Expo custom dev client install paths
- README.md links to the new docs without losing existing content
- All artifacts committed
</success_criteria>

<output>
Create `.planning/phases/06-explore-and-define-the-features-of-this-package-and-how-to-u/06-02-SUMMARY.md` when done
</output>
